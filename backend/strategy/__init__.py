"""Strategy module — multi-leg options strategies with risk management.

See ``docs/plan/strategy-module.md`` for the full design. Phase 1 ships the
DB schema, Pydantic contracts, repository layer, and CRUD endpoints; later
phases add the engine, risk evaluation, scheduler, and TradingView webhook.
"""
